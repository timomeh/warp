import glamorous from 'glamorous'

const Card = glamorous.div({
  background: '#FFFFFF',
  boxShadow: '0 3px 4px 0 rgba(0,0,0,0.12), 0 0 4px 0 rgba(0,0,0,0.14)',
  borderRadius: 2
})

export default Card
