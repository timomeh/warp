import React from 'react'
import PropTypes from 'prop-types'

const SideBar = props => {
  const { } = props

  return (
      <div>
        <p>Sidebar</p>
      </div>
  )
}

SideBar.propTypes = {

}

export default SideBar
